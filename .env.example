DB_URL=jdbc:postgresql://postgres:5432/automationhub
DB_USER=automationhub
DB_PASSWORD=changeme
JWT_SECRET=replace-with-a-long-random-base64-encoded-secret-at-least-32-bytes
JWT_EXPIRATION=3600000

# Optional. Set to a real Slack Incoming Webhook URL to enable live Slack delivery
# (SlackSender + SLACK action type). Leave blank for log-only behavior.
SLACK_WEBHOOK_URL=
